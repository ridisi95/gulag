import React from 'react'
import HeaderContainer from "./HeaderContainer";

const Header = () => {
    return (
        <header>
            <HeaderContainer/>
        </header>
    );
}
export default Header;